# Project workflow

- After every requested game update is implemented and verified, commit it to `main`, push it to `origin`, wait for the GitHub Pages workflow to succeed, and verify the live page at `https://kun-cmd.github.io/superidol/`.
- Skip publishing only when the user explicitly asks to keep an update local or not to publish it.
